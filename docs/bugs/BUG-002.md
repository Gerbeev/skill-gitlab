# [P2] BUG-002: Optional-field instructions are ignored by standalone extraction

- **Status:** Fixed; verified in the working tree on 2026-09-23.
- **Severity:** Major.
- **Component/location:** `issues.py / optional_empty and analyze_issue` under `skills/_engine/src/mr_impact`.
- **Requirements:** [TASK_STATEMENT](../TASK_STATEMENT.md), section 2.
- **Environment:** Windows, Python 3.14, local Git and SQLite; no GitLab calls or operational job execution.
- **Affected baseline:** working copy audited over `8b020a2372ff400d8fd69bfc7953530398fe00bb`, including then-existing edits.
- **Owner:** unassigned. This is a local engineering record, not a published external issue.

## Original reproduction and actual behavior

Use `<!-- Leave empty when no evidence exists. -->` above an unsupported optional field. The original standalone renderer inserted an Unresolved message into that field.

The original observed output is retained in [audit-results-before.json](audit-results-before.json).
The complete runnable scenario is in [audit-repro.py](../../skills/_engine/scripts/audit-repro.py).

## Expected behavior and impact

Honor the empty-field directive and distinguish a deterministic draft from a completed semantic review. The original behavior violated the cited evidence/format contract; priority
reflects the impact on trustworthy Issue or MR analysis. Severity is not an assertion
of remote code execution or an exploitable security vulnerability.

## Implemented correction

A narrow empty-if-unsupported directive is recognized. Arbitrary instructions require a source-bound agent review. Standalone results are explicitly drafts; --require-review rejects missing or incomplete review before writing.

## Acceptance and regression coverage

`Optional-field and review-gate tests: test_optional_instruction_and_draft_review_gate, test_review_binds_sources_decisions_and_instructions` in [test_regressions.py](../../skills/_engine/tests/test_regressions.py).
The original probe now reports `reproduced: false`; [audit-results.json](audit-results.json)
contains the current observations. Additional limits are described in the workflow
and [implementation follow-up](../IMPROVEMENT_INSTRUCTIONS.md).

From the repository root:

```text
python -m unittest discover -s skills/_engine/tests -t skills/_engine -p test_regressions.py -v
python skills/_engine/scripts/audit-repro.py
```

Both commands must succeed. Do not close future regressions by weakening the asserted
expected behavior or silently dropping uncertain evidence.
